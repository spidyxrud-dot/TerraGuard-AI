# TerraGuard AI - locked backend + ML stack (Python 3.12)
# Install with: backend/.venv/Scripts/python.exe -m pip install -r backend/requirements.txt
# torch/torchvision are installed first (see scripts/setup_env.ps1) so this file sees them as satisfied.

# --- Web API ---
fastapi>=0.115,<2.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
python-multipart>=0.0.12

# --- Data science core ---
numpy>=2.0,<3.0
pandas>=2.2,<3.0
scikit-learn>=1.5,<2.0
opencv-python>=4.10,<5.0

# --- Boosted trees + explainability ---
xgboost>=2.1,<4.0
shap>=0.46,<1.0

# --- Geospatial I/O (Satellite/GIS) ---
rasterio>=1.3,<2.0
geopandas>=1.0,<2.0
shapely>=2.0,<3.0
pyproj>=3.6,<4.0

# --- Database ---
sqlalchemy>=2.0,<3.0
psycopg[binary]>=3.2,<4.0
alembic>=1.13,<2.0

# --- Deep learning (CPU build on Windows) ---
torch>=2.5,<3.0
torchvision>=0.20,<1.0

# --- Tooling / tests ---
httpx>=0.27,<1.0
pytest>=8.0,<9.0

